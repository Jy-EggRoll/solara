# Git
.git
.github

# Node 依赖（容器内由 pnpm 重新安装）
node_modules
server/node_modules

# 运行时数据（通过 volume 挂载，不应打包进镜像）
data
*.db
*.db-shm
*.db-wal

# 构建产物与体积较大的仓库示例图（不进镜像 / 不膨胀构建上下文）
dist
.pnpm-store
Preview.gif
1.png
2.png
3.png

# 敏感配置（运行时由 entrypoint 动态生成）
.dev.vars

# 系统文件
.DS_Store
Thumbs.db
